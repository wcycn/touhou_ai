# Changelog

## 1.1.0 - 2026-07-28

- 建立独立、可发布的干净目录。
- 只保留新的统一桌面控制中心，移除旧 GUI 和在线 Advisor。
- 增加安全观察、动态窗口定位、焦点守护与输入测试。
- 增加结构化会话记录、GUI回放和离线报告。
- 将实际控制核心命名为 `autopilot.py`。
- 移除不参与推理的 MindSpore 装饰性代码。
- 不再强制关闭 PyTorch CUDA。
- 游戏本体不进入发布目录。
- 增加边界保护、方向保持与反向冷却。
- 增加差分按键状态机和焦点丢失立即释放。
- 增加自机跟踪、短时预测和漏检超时停控。
- 增加敌弹跟踪、速度、TTC、轨迹风险和四方向规划。
- 增加保守场景状态机，只在确认战斗时允许控制。
- 会话格式升级至v2，记录轨迹、风险、场景和按键差异。
- 增加会话质量分析、人工审核候选和预标注导出。
- 增加人工标注YOLO数据集的模型评估入口。
- 重排GUI首页，将AI模拟观察和AI自动控制设为两个主要入口。
- 增加持续可见的当前输入模式和红色停止/释放按键按钮。
- 观察与控制共用经过测试的参数构造函数，避免命令参数漂移。
