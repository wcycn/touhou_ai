# 游戏文件目录

发布包不包含《东方红魔乡》游戏本体。

请将你合法取得、允许在本机使用的游戏文件复制到此目录。启动器会按以下顺序寻找：

1. `vpatch.exe`
2. `th06c.exe`
3. `th06.exe`
4. `東方紅魔郷.exe`

如果游戏包明确要求 vpatch，请同时复制相应 DLL、配置和数据文件，保持原目录结构。
这些文件已被 `.gitignore` 排除，不会在普通 Git 提交中误上传。

